#include "main.h"

/**
 * print_numbers - Print the numbers 0 to 9
 * Return: Always (Success)
 */
void print_numbers(void)
{
	int a;

	for (a == 0; a <= 9; a++)
	{
		_putchar(a);
	}
	_putchar('\n');
	return (0);
}

