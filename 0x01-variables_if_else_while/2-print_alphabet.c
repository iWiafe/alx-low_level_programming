#include <stdio.h>

/**
 * main - Programme that prints alphabet in lower case
 * Return: Always (Success)
 */
int main(void)
{
	int ch;

	for (ch  = 'a'; ch  <= 'z'; ch++)
	putchar(ch);
	putchar('\n');
	return (0);
}
