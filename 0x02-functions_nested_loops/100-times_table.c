#include "main.h"

/**
 * print_times_table - Prints the n times table
 * @n: n integer as an input
 * Return: void
 */
void print_times_table(int n)
{
	int n;
	int i;
	int a;

	n = 0;
	a = n * i;
	for (i = 0; i <= 12; i++)
	{
		if (!(n > 15 || n < 0))
		_putchar(a);
	}
	_putchar('\n');
}
