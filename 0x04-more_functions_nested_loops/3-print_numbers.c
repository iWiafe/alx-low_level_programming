#include "main.h"

/**
 * print_numbers - Print the numbers 0 to 9
 * Return: Always (Success)
 */
void print_numbers(void)
{
	int a = 0;

	for (a <= 9; a++)
	{
		_putchar(a);
	}
	_putchar('\n');
	return (0);
}

